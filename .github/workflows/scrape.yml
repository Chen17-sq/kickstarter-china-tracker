name: scrape

on:
  schedule:
    # 01:00 UTC = 09:00 Beijing — fresh for morning. One snapshot/day.
    - cron: "0 1 * * *"
  workflow_dispatch:        # manual run from Actions tab

permissions:
  contents: write

concurrency:
  group: scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Chromium for PDF render
        run: python -m playwright install --with-deps chromium

      - name: Run scraper
        env:
          # If the secret is unset, scraper.translate logs a warning and skips
          # auto-translation — pipeline still produces correct projects.json.
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m scraper.run

      - name: Commit changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "ks-tracker-bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ reports/ assets/ site/editions/ site/sitemap.xml site/robots.txt
          [ -f CHANGELOG.md ] && git add CHANGELOG.md || true
          if git diff --staged --quiet; then
            echo "No changes."
            exit 0
          fi
          git commit -m "snapshot $(date -u +%FT%TZ)"
          git push

      - name: Slack / Discord notify (optional)
        if: env.SLACK_WEBHOOK != '' || env.DISCORD_WEBHOOK != ''
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: python -m scraper.notify

      - name: Email notify (optional)
        if: env.RESEND_API_KEY != '' && env.NOTIFY_EMAIL_TO != ''
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          NOTIFY_EMAIL_TO: ${{ secrets.NOTIFY_EMAIL_TO }}
          NOTIFY_EMAIL_FROM: ${{ secrets.NOTIFY_EMAIL_FROM }}
        run: python -m scraper.email_notify
